[package]
name = "wasm-runtime-types"
version = "0.1.0"
edition = "2024"

[dependencies]
pool = { path = "../pool" }

bincode = "2"
sendable = "0.6"
serde = "1"
wasmer = { version = "7", default-features = false, features = [
  "sys",
  "cranelift",
] }
