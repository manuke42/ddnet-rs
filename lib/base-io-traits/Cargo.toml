[package]
name = "base-io-traits"
version = "0.1.0"
edition = "2024"

[dependencies]
base = { path = "../base" }

anyhow = { version = "1", features = ["backtrace"] }
async-trait = "0.1"
bytes = "1"
rustc-hash = "2"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
url = "2"
